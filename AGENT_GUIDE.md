# 🤖 小白必看：如何指揮你的 AI 學習這個 GAS 同步技能？

如果你是剛接觸 AI Agent（如 Cursor, Windsurf, Antigravity 等）的初學者，不知道該怎麼讓你的 AI 學會這個「修改完 GAS 程式碼自動同步，免複製貼上」的神技，請跟著以下步驟做！

---

## 🚀 如何讓你的 AI 載入技能？（兩種方式任選）

### 💡 方式 A：免下載！直接丟網址給 AI 學習（最推薦、最簡單）

現在的 AI Agent 非常聰明，只要你提供網址，它就能自己上網讀取技能。

用你的 AI 編輯器（如 Cursor, Windsurf, Antigravity）開啟你的開發專案後，直接在對話框**複製貼上這段話**給它：

> 💬 **給 AI 的指令（直接複製）**：
> 「請你上網讀取這個網址：`https://github.com/puti001/gas-clasp-workflow`。
> 
> 學習裡面關於使用 `clasp` 工具進行自動同步開發的 **Skill** 與 **Rule**。**未來只要我進行 Google Apps Script (GAS) 開發**，一旦你在本地修改或新增了任何代碼（包括前端 .html 或後端 .js 檔案），修改完請自動在終端機執行 `npx @google/clasp push` 同步至雲端，免去我手動複製貼上。請回覆我『我已理解此技能，隨時準備好進行 GAS 開發』。」

---

### 📦 方式 B：將技能包放入專案中（長期穩定）

如果你想讓 AI 每次開啟專案時都自動記住這個技能，不需要每次貼網址：

1. 下載這個 GitHub 專案的所有檔案（點擊右上方 **Code** -> **Download ZIP** 並解壓縮）。
2. 把解壓出來的 **`.agents`** 資料夾，複製並整包貼進你的 GAS 開發專案根目錄下。
3. 直接對 AI 說：
   > 💬 「請讀取我根目錄下 `.agents/` 內的技能與規則。往後代碼修改完畢後，請自動執行 `npx @google/clasp push` 同步到雲端。」
此時，你的 AI 就會讀取規則，並且「聽話」地載入這個自動同步的工作流。

---

## 步驟三：日常開發的指揮暗號（實戰語句）

當你的 AI 學會這個技能後，你可以用以下這幾句「魔法指令」指揮它：

### 🎯 暗號 1：把雲端的現有專案拉下來
> 💬 「幫我 clone 雲端專案，Script ID 是 `你的_SCRIPT_ID`」
> *（AI 會在本地下載你雲端本來的程式碼）*

### 🎯 暗號 2：請它寫程式並自動上傳
> 💬 「幫我在 Google 試算表新增一個選單按鈕，並在本地端寫好代碼後直接 push 到 GAS 雲端」
> *（AI 寫完代碼後，會自動在背景執行 push，你只需去瀏覽器重新整理 GAS 網頁即可看見最新成果！）*

### 🎯 暗號 3：從雲端把最新代碼拉回本地
> 💬 「我剛剛在網頁端改了代碼，幫我 pull 回本地更新檔案」

---

## ⚠️ 常見的踩坑與解決辦法 (Q&A)

### Q1：AI 執行 `clasp` 時報錯「Google Apps Script API is not enabled...」？
* **解法**：這是因為你的 Google 帳號還沒開啟 API 權限。請用瀏覽器打開這個網址：[Google Apps Script 使用者設定](https://script.google.com/home/usersettings)，並將最下方的 **Google Apps Script API** 切換為 **啟用 (ON)** 即可。

### Q2：AI 執行 `clasp` 時顯示未登入？
* **解法**：在你的電腦終端機手動輸入以下指令，瀏覽器會跳出視窗，讓你點選你的 Google 帳號完成登入授權即可！
  ```bash
  clasp login
  ```

### Q3：GAS 不是通常有前端 `index.html` 和後端 `code.gs` 兩處程式碼嗎？本地端要怎麼對應？
* **解法**：沒錯！在本地端：
  - **後端**程式碼：本地使用 `.js` 檔（如 `code.js`），`clasp` 推送時會**自動**將其轉換為雲端的 `.gs` 檔（如 `code.gs`）。
  - **前端**程式碼：本地保持使用 `.html` 檔（如 `index.html`）。
  - 當 AI 在背景執行 `clasp push` 時，會**一次性將本地的前後端所有檔案打包上傳**，雲端的前後端會同步更新，完全不需要手動分別複製貼上！
